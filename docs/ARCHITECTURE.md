# Архитектура SberLab Hub

Документ описывает детальную архитектуру проекта, технологические решения, их преимущества и недостатки.

---

## Содержание

- [Общий обзор](#общий-обзор)
- [Frontend архитектура](#frontend-архитектура)
- [Backend архитектура](#backend-архитектура)
- [База данных](#база-данных)
- [Кеширование](#кеширование)
- [Семантический поиск](#семантический-поиск)
- [Аутентификация и авторизация](#аутентификация-и-авторизация)
- [Плюсы и минусы решений](#плюсы-и-минусы-решений)

---

## Общий обзор

### Архитектурный стиль

**Three-tier architecture (трехуровневая архитектура)**

```
┌─────────────────────────────────────────────────────────────────┐
│                    Presentation Layer                            │
│                    (React + Vite + MUI)                          │
│                                                                  │
│  • Отображение данных                                           │
│  • Валидация форм                                               │
│  • Управление состоянием (Context API)                          │
│  • Маршрутизация (React Router)                                │
└────────────────────────┬────────────────────────────────────────┘
                         │ REST API (JSON + JWT)
┌────────────────────────┴────────────────────────────────────────┐
│                   Application Layer                              │
│                   (Quarkus + CDI)                                │
│                                                                  │
│  • Business Logic (Services)                                    │
│  • REST Controllers (Resources)                                 │
│  • DTO маппинг                                                  │
│  • Валидация (Bean Validation)                                 │
│  • Кеширование (Redis)                                          │
│  • Events (CDI Event Bus)                                       │
└────────────────────────┬────────────────────────────────────────┘
                         │ JDBC / Redis Protocol
┌────────────────────────┴────────────────────────────────────────┐
│                     Data Layer                                   │
│                                                                  │
│  ┌──────────────────┐        ┌──────────────────┐              │
│  │   PostgreSQL 16  │        │    Redis 7       │              │
│  │   + pgvector     │        │   (Cache)        │              │
│  └──────────────────┘        └──────────────────┘              │
│                                                                  │
│  • Persistent storage                                           │
│  • Vector search (pgvector)                                     │
│  • Query embeddings cache (Redis)                               │
│  • Project details cache (Redis)                                │
└─────────────────────────────────────────────────────────────────┘
```

### Коммуникация между слоями

1. **Frontend → Backend**: HTTP REST API
   - JSON формат данных
   - JWT токен в заголовке `Authorization: Bearer <token>`
   - CORS настроен для `http://localhost:5173`

2. **Backend → Database**: JDBC через Hibernate ORM
   - Panache Active Record паттерн
   - Автоматические миграции через Flyway

3. **Backend → Redis**: Redis Protocol
   - Quarkus Redis Cache extension
   - Автоматическая сериализация/десериализация

4. **Backend → GigaChat API**: HTTP REST
   - OAuth 2.0 аутентификация
   - Генерация embeddings для семантического поиска

---

## Frontend архитектура

### Технологический стек

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| React | 18.3 | UI библиотека |
| Vite | 5.4 | Build tool и dev server |
| MUI (Material-UI) | 5.16 | Component library |
| React Router | 6.26 | Client-side routing |
| Emotion | 11.11 | CSS-in-JS |

### Структура

```
frontend/src/
├── main.jsx                  # Entry point
│   └── App.jsx               # Root component с роутами
│
├── contexts/                 # React Context API
│   ├── AuthContext.jsx       # JWT + user state
│   ├── LanguageContext.jsx   # i18n (EN/RU)
│   ├── ThemeContext.jsx      # Dark/Light mode
│   └── SnackbarContext.jsx   # Toast notifications
│
├── components/
│   ├── Layout.jsx            # AppBar + Sidebar
│   └── ProtectedRoute.jsx    # Role-based route guards
│
├── pages/                    # Page components
│   ├── LoginPage.jsx
│   ├── CatalogPage.jsx
│   ├── ProjectDetailPage.jsx
│   ├── CreateProjectPage.jsx
│   ├── MyApplicationsPage.jsx
│   ├── MentorDashboard.jsx
│   ├── MentorProjectApplications.jsx
│   ├── AdminUsersPage.jsx
│   ├── AdminProjectsPage.jsx
│   └── AdminAllowListPage.jsx
│
├── api.js                    # HTTP client wrapper
└── translations.js           # i18n strings
```

### Паттерны и подходы

#### 1. Context API для глобального состояния

**AuthContext** — управление аутентификацией:
```javascript
const { user, loading, login, logout } = useAuth();
```

**Преимущества:**
- ✅ Нет необходимости в Redux для простого приложения
- ✅ Встроенная поддержка React
- ✅ Легко тестировать

**Недостатки:**
- ❌ Re-render всех потребителей контекста при изменении
- ❌ Нет DevTools для отладки
- ⚠️ **Решение**: Разделение контекстов по доменам (Auth, Language, Theme)

#### 2. Защищенные маршруты

```jsx
<ProtectedRoute roles={['ADMIN']}>
  <AdminUsersPage />
</ProtectedRoute>
```

- Проверка роли перед рендером
- Редирект на `/login` для неавторизованных
- Редирект на `/` для недостаточных прав

#### 3. API Client wrapper

```javascript
// Автоматическая инъекция JWT
api.get('/api/projects')
api.post('/api/projects', { title: '...' })

// Автоматическая обработка ошибок
401 → Редирект на /login + очистка токена
403 → Parse error message из body
```

#### 4. Интернационализация (i18n)

**Подход:** Custom solution на базе Context API

```javascript
const { t, language, toggleLanguage } = useLanguage();
<Button>{t('nav.catalog')}</Button>
```

**Плюсы:**
- ✅ Простота (без внешних библиотек)
- ✅ Быстрое переключение языков

**Минусы:**
- ❌ Нет плюрализации
- ❌ Нет интерполяции переменных
- ❌ Все переводы загружаются сразу
- ⚠️ **Альтернатива**: react-i18next для больших проектов

#### 5. Debounce для поисковых запросов

```javascript
// Задержка 500ms перед отправкой запроса
useEffect(() => {
  const timer = setTimeout(() => {
    setQ(searchInput);
  }, 500);
  return () => clearTimeout(timer);
}, [searchInput]);
```

**Зачем:** Уменьшает нагрузку на backend при печати

---

## Backend архитектура

### Технологический стек

| Технология | Версия | Назначение |
|------------|--------|------------|
| Java | 25 (preview) | Язык программирования |
| Quarkus | 3.31.1 | REST API фреймворк |
| Hibernate ORM | 6.4 | ORM |
| Panache | - | Active Record паттерн |
| SmallRye JWT | - | JWT authentication |
| Flyway | 10.x | Database migrations |
| Jakarta Bean Validation | 3.0 | Валидация DTO |

### Слои приложения

```
┌─────────────────────────────────────────────────────────────┐
│                      Resources Layer                         │
│  (REST Controllers — JAX-RS @Path, @GET, @POST, ...)        │
│                                                              │
│  • Обработка HTTP запросов                                  │
│  • Валидация входных данных (@Valid)                        │
│  • Маппинг DTO ↔ Entities                                   │
│  • Role-based access control (@RolesAllowed)                │
│  • Exception handling (GlobalExceptionMapper)               │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────────┐
│                      Services Layer                          │
│  (Business Logic — @ApplicationScoped beans)                │
│                                                              │
│  • ProjectSearchService — семантический поиск               │
│  • QueryEmbeddingCache — кеширование embeddings             │
│  • GigaChatService — интеграция с GigaChat API              │
│  • JwtService — генерация JWT токенов                       │
│  • DataSeeder — начальные данные                            │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────┴────────────────────────────────────┐
│                    Entities Layer                            │
│  (Domain Models — Hibernate Panache @Entity)                │
│                                                              │
│  • User, Project, Application, Question, Answer, Feedback   │
│  • Active Record methods: persist(), findById(), list()     │
│  • Relationships: @ManyToOne, @OneToMany                    │
└─────────────────────────────────────────────────────────────┘
```

### Ключевые решения

#### 1. Hibernate Panache (Active Record)

**Традиционный подход (Repository):**
```java
@Repository
public class ProjectRepository {
    public Project findById(Long id) { ... }
    public List<Project> findAll() { ... }
}
```

**Panache (Active Record):**
```java
Project p = Project.findById(1L);
List<Project> all = Project.listAll();
p.persist();
```

**Плюсы:**
- ✅ Меньше boilerplate кода
- ✅ Более читаемый код
- ✅ Публичные поля вместо getters/setters

**Минусы:**
- ❌ Нарушает принцип разделения ответственности (SRP)
- ❌ Сложнее тестировать (entity зависит от БД)
- ❌ Не подходит для Domain-Driven Design

#### 2. Java 25 с preview features

**Используемые фичи:**
- **Virtual Threads** — легковесные потоки для параллельных операций
- **Vector API** — SIMD оптимизация для вычисления L2 distance

**Пример:**
```java
@RunOnVirtualThread
public Response getFullDetails(Long id) {
    // Выполняется на виртуальном потоке
    CompletableFuture<...> future = CompletableFuture.supplyAsync(
        () -> fetchData(),
        virtualExecutor // Executor с виртуальными потоками
    );
}
```

**Плюсы:**
- ✅ Миллионы потоков без проблем с памятью
- ✅ Простой код для асинхронности (как синхронный)
- ✅ SIMD ускорение векторных операций в 4-8 раз

**Минусы:**
- ❌ Требуются флаги JVM: `--enable-preview --add-modules jdk.incubator.vector`
- ❌ Не все IDE поддерживают preview features
- ❌ Может быть нестабильно (preview status)

#### 3. CDI Events для async операций

**Проблема:** Генерация embeddings долгая (~1-2 секунды), блокирует транзакцию

**Решение:** CDI Event Bus + async processing

```java
// В ресурсе
@Transactional
public Response update(Long id) {
    Project p = Project.findById(id);
    p.title = "New title";

    // Firing event ПОСЛЕ коммита транзакции
    projectEvent.fire(new ProjectEvent(p.id, UPDATED));

    return Response.ok().build();
}

// В сервисе
public void onProjectEvent(
    @Observes(during = TransactionPhase.AFTER_SUCCESS) ProjectEvent event
) {
    // Запускается ПОСЛЕ commit в отдельном потоке
    executorService.submit(() -> {
        generateEmbedding(event.getProjectId());
    });
}
```

**Плюсы:**
- ✅ Не блокирует основную транзакцию
- ✅ Нет проблем с detached entities
- ✅ Retry логика легко добавляется

**Минусы:**
- ❌ Сложнее отлаживать (асинхронность)
- ❌ Нужен отдельный thread pool

#### 4. DTO Pattern

**Зачем:**
- Отделить API контракт от domain model
- Скрыть внутренние поля (пароли, метаданные)
- Контролировать сериализацию JSON

**Пример:**
```java
public record ProjectDto(
    Long id,
    String title,
    String goal,
    // ...
) {
    public static ProjectDto from(Project entity) {
        return new ProjectDto(
            entity.id,
            entity.title,
            entity.goal
            // ...
        );
    }
}
```

**Плюсы:**
- ✅ Иммутабельность (Java records)
- ✅ Явный контроль над API
- ✅ Версионирование API проще

**Минусы:**
- ❌ Дублирование кода (entity + DTO)
- ❌ Ручной маппинг (без MapStruct)

---

## База данных

### PostgreSQL 16 + pgvector

**Схема:**

```sql
-- Пользователи
CREATE TABLE users (
    id BIGSERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    name VARCHAR(255),
    role VARCHAR(20) NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Проекты
CREATE TABLE projects (
    id BIGSERIAL PRIMARY KEY,
    mentor_id BIGINT REFERENCES users(id),
    title VARCHAR(500) NOT NULL,
    goal TEXT,
    key_tasks TEXT,
    tags TEXT,
    skills TEXT,
    difficulty VARCHAR(20),
    status VARCHAR(20) DEFAULT 'DRAFT',
    embedding vector(1024),  -- pgvector для семантического поиска
    created_at TIMESTAMP DEFAULT NOW()
);

-- Заявки студентов
CREATE TABLE applications (
    id BIGSERIAL PRIMARY KEY,
    project_id BIGINT REFERENCES projects(id),
    student_id BIGINT REFERENCES users(id),
    message TEXT,
    status VARCHAR(20) DEFAULT 'PENDING',
    created_at TIMESTAMP DEFAULT NOW()
);

-- Вопросы
CREATE TABLE questions (
    id BIGSERIAL PRIMARY KEY,
    project_id BIGINT REFERENCES projects(id),
    author_id BIGINT REFERENCES users(id),
    text TEXT NOT NULL,
    visibility VARCHAR(20) DEFAULT 'PUBLIC',
    created_at TIMESTAMP DEFAULT NOW()
);

-- Ответы
CREATE TABLE answers (
    id BIGSERIAL PRIMARY KEY,
    question_id BIGINT REFERENCES questions(id),
    responder_id BIGINT REFERENCES users(id),
    text TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Обратная связь
CREATE TABLE feedback (
    id BIGSERIAL PRIMARY KEY,
    project_id BIGINT REFERENCES projects(id),
    student_id BIGINT REFERENCES users(id),
    type VARCHAR(20),
    rating INT CHECK (rating BETWEEN 1 AND 5),
    comment TEXT,
    created_at TIMESTAMP DEFAULT NOW()
);

-- Allow List
CREATE TABLE allow_list (
    id BIGSERIAL PRIMARY KEY,
    email VARCHAR(255) UNIQUE NOT NULL,
    role VARCHAR(20) NOT NULL
);
```

### Индексы

```sql
CREATE INDEX idx_projects_mentor ON projects(mentor_id);
CREATE INDEX idx_projects_status ON projects(status);
CREATE INDEX idx_applications_project ON applications(project_id);
CREATE INDEX idx_applications_student ON applications(student_id);
CREATE INDEX idx_questions_project ON questions(project_id);
CREATE INDEX idx_answers_question ON answers(question_id);

-- Vector index для pgvector (ускоряет семантический поиск)
CREATE INDEX idx_projects_embedding ON projects
    USING ivfflat (embedding vector_l2_ops)
    WITH (lists = 100);
```

### Миграции (Flyway)

Все изменения схемы версионируются через SQL миграции:

```
backend/src/main/resources/db/migration/
├── V1__init.sql           # Основные таблицы
├── V2__allow_list.sql     # Таблица allow_list
└── V3__add_vector_search.sql  # pgvector extension + embedding column
```

**Плюсы:**
- ✅ Version control для БД
- ✅ Rollback возможен (через специальные скрипты)
- ✅ Автоматический запуск при старте

**Минусы:**
- ❌ Нельзя изменить уже примененную миграцию
- ❌ Нужно быть осторожным с production БД

### Cascade DELETE

**Решение:** Ручной cascade в коде (НЕ через БД)

**Пример:**
```java
// DELETE /api/admin/projects/{id}
@Transactional
public Response deleteProject(Long id) {
    // 1. Удалить ответы
    Answer.delete("question.project.id", id);

    // 2. Удалить вопросы
    Question.delete("project.id", id);

    // 3. Удалить заявки
    Application.delete("project.id", id);

    // 4. Удалить обратную связь
    Feedback.delete("project.id", id);

    // 5. Удалить проект
    Project.deleteById(id);

    return Response.noContent().build();
}
```

**Плюсы:**
- ✅ Явный контроль над удалением
- ✅ Можно добавить логирование
- ✅ Транзакционность гарантирована

**Минусы:**
- ❌ Риск забыть удалить связанные данные
- ❌ Orphaned records при ошибках
- ⚠️ **Решение:** Интеграционные тесты на удаление

---

## Кеширование

### Redis 7 Alpine

**Архитектура кеша:**

```
┌──────────────────────────────────────────────────┐
│              Application (Quarkus)                │
│                                                   │
│  ┌─────────────────────────────────────────────┐ │
│  │  @CacheResult(cacheName = "project-details") │ │
│  │  ProjectDto getCachedProjectDto(Long id)     │ │
│  └──────────────────┬───────────────────────────┘ │
│                     │                             │
│  ┌──────────────────┴───────────────────────────┐ │
│  │  @CacheResult(cacheName = "query-embeddings")│ │
│  │  List<Float> getQueryEmbedding(String query) │ │
│  └──────────────────┬───────────────────────────┘ │
└───────────────────────┼──────────────────────────┘
                        │
              ┌─────────┴─────────┐
              │   Redis (Cache)   │
              │                   │
              │  Keys:            │
              │  • sberlab:project-details:1     │
              │  • sberlab:query-embeddings:ml   │
              └───────────────────┘
```

### Стратегии кеширования

#### 1. Кеширование деталей проекта

**TTL:** 1 час

**Инвалидация:**
- При обновлении проекта (`PUT /api/projects/{id}`)
- При публикации (`POST /api/projects/{id}/publish`)
- При архивации (`POST /api/projects/{id}/archive`)

**Код:**
```java
@CacheResult(cacheName = "project-details")
ProjectDto getCachedProjectDto(Long id) { ... }

@CacheInvalidate(cacheName = "project-details")
public Response update(@PathParam("id") Long id) { ... }
```

**Метрики:**
- Cache miss: ~50-100ms (SQL query)
- Cache hit: ~2-10ms (Redis)
- **Ускорение: 5-20x**

#### 2. Кеширование query embeddings

**TTL:** 7 дней

**Зачем так долго?**
- Embeddings детерминированы (один текст → один вектор)
- API вызов к GigaChat дорогой (~500-2000ms)
- Популярные запросы повторяются

**Код:**
```java
@CacheResult(cacheName = "query-embeddings")
public List<Float> getQueryEmbedding(String queryText) {
    String normalized = queryText.trim().toLowerCase();
    return gigaChatService.generateEmbedding(normalized);
}
```

**Метрики:**
- Cache miss: ~500-2000ms (GigaChat API)
- Cache hit: ~1-5ms (Redis)
- **Ускорение: 200-1000x** 🚀

### Конфигурация Redis

```properties
# Connection
quarkus.redis.hosts=redis://localhost:6379
quarkus.redis.timeout=5s

# Global settings
quarkus.cache.type=redis
quarkus.cache.redis.prefix=sberlab

# TTL per cache
quarkus.cache.redis."project-details".expire-after-write=1h
quarkus.cache.redis."query-embeddings".expire-after-write=7d
```

**Eviction policy:** `allkeys-lru`
- При заполнении памяти удаляет самые старые ключи
- Max memory: 256MB (настроено в docker-compose.yml)

### Плюсы и минусы

**Плюсы:**
- ✅ Драматическое ускорение повторных запросов
- ✅ Уменьшение нагрузки на БД и внешние API
- ✅ Декларативный подход (аннотации)
- ✅ Автоматическая сериализация

**Минусы:**
- ❌ Дополнительная зависимость (Redis must be running)
- ❌ Риск stale data (если забыли инвалидировать)
- ❌ Memory overhead
- ❌ Сложность отладки (кеш может маскировать баги)

---

## Семантический поиск

### GigaChat Embeddings API

**Архитектура:**

```
User Query: "машинное обучение"
           ↓
┌──────────────────────────────────────┐
│  QueryEmbeddingCache.getQueryEmbedding│
└──────────┬───────────────────────────┘
           │
    ┌──────┴──────┐
    │   Redis?    │
    └──────┬──────┘
          / \
        Да   Нет
        ↓     ↓
     Вернуть  GigaChatService.generateEmbedding()
     из Redis         ↓
                 HTTP POST https://gigachat.devices.sberbank.ru/api/v1/embeddings
                      ↓
                 [0.123, -0.456, 0.789, ...]  (1024 floats)
                      ↓
                 Сохранить в Redis (TTL 7 дней)
                      ↓
                 Вернуть embedding
```

### Гибридный поиск (RRF)

**Reciprocal Rank Fusion** — комбинирование традиционного и семантического поиска

```
Запрос: "машинное обучение"

1. Traditional Search (SQL LIKE):
   ┌────────────────────────┬──────┐
   │ Project                │ Rank │
   ├────────────────────────┼──────┤
   │ ML for beginners       │  1   │
   │ Advanced ML            │  2   │
   │ Deep Learning          │  3   │
   └────────────────────────┴──────┘

2. Semantic Search (Embeddings + L2 distance):
   ┌────────────────────────┬──────┬──────────┐
   │ Project                │ Rank │ Distance │
   ├────────────────────────┼──────┼──────────┤
   │ Neural Networks        │  1   │  0.234   │
   │ ML for beginners       │  2   │  0.345   │
   │ Computer Vision        │  3   │  0.456   │
   └────────────────────────┴──────┴──────────┘

3. RRF Fusion (score = Σ 1/(60 + rank)):
   ┌────────────────────────┬─────────────────┐
   │ Project                │ RRF Score       │
   ├────────────────────────┼─────────────────┤
   │ ML for beginners       │ 1/61 + 1/62 = 0.0327 │
   │ Neural Networks        │ 1/61 = 0.0164        │
   │ Advanced ML            │ 1/62 = 0.0161        │
   │ Deep Learning          │ 1/63 = 0.0159        │
   │ Computer Vision        │ 1/63 = 0.0159        │
   └────────────────────────┴─────────────────┘

Результат: "ML for beginners" на первом месте (найден обоими методами)
```

### SIMD оптимизация (Java Vector API)

**L2 Distance вычисление:**

```java
// Без SIMD (классический цикл)
float distance = 0.0f;
for (int i = 0; i < a.length; i++) {
    float diff = a[i] - b[i];
    distance += diff * diff;
}
return (float) Math.sqrt(distance);

// С SIMD (Vector API)
var species = FloatVector.SPECIES_PREFERRED; // 256-bit AVX2 = 8 floats
float sum = 0.0f;
for (int i = 0; i < a.length; i += species.length()) {
    var va = FloatVector.fromArray(species, a, i);
    var vb = FloatVector.fromArray(species, b, i);
    var diff = va.sub(vb);
    sum += diff.mul(diff).reduceLanes(VectorOperators.ADD);
}
return (float) Math.sqrt(sum);
```

**Ускорение:** 4-8x на AVX2/AVX-512 процессорах

### Плюсы и минусы

**Плюсы:**
- ✅ Умный поиск по смыслу, а не только по словам
- ✅ Находит синонимы ("ML" → "machine learning")
- ✅ Кросс-языковой поиск (английский запрос → русский проект)
- ✅ SIMD ускорение вычислений

**Минусы:**
- ❌ Зависимость от внешнего API (GigaChat)
- ❌ Latency ~500-2000ms без кеша
- ❌ Требует Java Vector API (preview feature)
- ❌ Качество зависит от модели GigaChat
- ⚠️ **Решение:** Redis кеширование + fallback на традиционный поиск

---

## Аутентификация и авторизация

### JWT (JSON Web Tokens)

**Flow:**

```
1. Login Request
   POST /api/auth/google или /api/auth/dev-login
        ↓
   ┌─────────────────────────────────────┐
   │  Backend проверяет Google ID Token  │
   │  или принимает email+role в dev     │
   └──────────────┬──────────────────────┘
                  ↓
   ┌─────────────────────────────────────┐
   │  Генерация JWT с claims:            │
   │  • sub: user_id                     │
   │  • groups: [STUDENT/MENTOR/...]     │
   │  • exp: now + 24 hours              │
   └──────────────┬──────────────────────┘
                  ↓
   ┌─────────────────────────────────────┐
   │  Подпись RSA приватным ключом       │
   └──────────────┬──────────────────────┘
                  ↓
   Response: { token: "eyJhbGc...", user: {...} }

2. Authenticated Request
   GET /api/projects
   Header: Authorization: Bearer eyJhbGc...
        ↓
   ┌─────────────────────────────────────┐
   │  Backend проверяет подпись          │
   │  публичным ключом                   │
   └──────────────┬──────────────────────┘
                  ↓
   ┌─────────────────────────────────────┐
   │  Извлекает user_id и role из token  │
   └──────────────┬──────────────────────┘
                  ↓
   ┌─────────────────────────────────────┐
   │  @RolesAllowed проверяет доступ     │
   └──────────────┬──────────────────────┘
                  ↓
   200 OK с данными
```

### Role-Based Access Control

**Аннотации:**
```java
@GET
@Path("/projects")
@RolesAllowed({"STUDENT", "TEACHER", "MENTOR", "ADMIN"})
public Response list() { ... }

@POST
@Path("/projects")
@RolesAllowed({"MENTOR", "ADMIN"})
public Response create() { ... }

@GET
@Path("/admin/users")
@RolesAllowed({"ADMIN"})
public Response listUsers() { ... }
```

### Google OIDC

**Production flow:**
1. Frontend загружает Google Identity Services
2. Пользователь нажимает "Sign in with Google"
3. Google показывает диалог авторизации
4. Google возвращает ID Token
5. Frontend отправляет ID Token на backend
6. Backend проверяет токен через Google API
7. Backend проверяет email в allow_list
8. Backend генерирует JWT и возвращает клиенту

### Dev Auth Mode

**Development flow:**
1. Frontend показывает форму email + role dropdown
2. POST /api/auth/dev-login с email и role
3. Backend БЕЗ ПРОВЕРКИ создает/находит пользователя
4. Backend генерирует JWT
5. Frontend сохраняет токен в localStorage

**⚠️ ВАЖНО:** Dev Auth должен быть отключен в production!

### Плюсы и минусы

**JWT Плюсы:**
- ✅ Stateless (не нужно хранить сессии)
- ✅ Легко масштабируется горизонтально
- ✅ Работает с мобильными приложениями
- ✅ Single Sign-On (SSO) с Google

**JWT Минусы:**
- ❌ Нельзя отозвать токен до истечения срока
- ❌ Токен может быть украден (XSS)
- ❌ Размер токена больше, чем session ID
- ⚠️ **Решение:** Короткий TTL (24 часа), HttpOnly cookies (опционально)

**Google OIDC Плюсы:**
- ✅ Не нужно хранить пароли
- ✅ 2FA от Google бесплатно
- ✅ Знакомый UX для пользователей

**Google OIDC Минусы:**
- ❌ Зависимость от Google (vendor lock-in)
- ❌ Требует регистрацию приложения в Google Cloud
- ❌ Студенты без @nsu.ru/@g.nsu.ru email не могут зарегистрироваться

---

## Плюсы и минусы решений

### Общая таблица

| Решение | Плюсы | Минусы | Альтернативы |
|---------|-------|--------|--------------|
| **Monorepo** | Единая кодовая база, синхронизация изменений | Сложнее разделить команды | Polyrepo (отдельные репозитории) |
| **Quarkus** | Быстрый старт, низкая память, Dev Mode | Меньше комьюнити, чем Spring | Spring Boot, Micronaut |
| **Panache** | Меньше кода, Active Record | Нарушает SRP, сложнее тестировать | Repository pattern |
| **Java 25** | Virtual threads, Vector API | Preview features нестабильны | Java 21 LTS |
| **Redis Cache** | Драматическое ускорение | Дополнительная зависимость | Caffeine (in-memory) |
| **GigaChat** | Умный семантический поиск | Зависимость от API, latency | Elasticsearch, OpenAI |
| **JWT** | Stateless, масштабируемость | Нельзя отозвать | Session cookies |
| **No TypeScript** | Быстрее разработка | Больше runtime ошибок | TypeScript, JSDoc |
| **Manual Cascade** | Явный контроль | Риск orphaned records | ON DELETE CASCADE |

---

## Рекомендации для production

### Security
- [ ] Отключить DEV_AUTH
- [ ] Включить HTTPS
- [ ] Настроить CORS только для production domains
- [ ] Ротация JWT ключей
- [ ] Rate limiting для API
- [ ] SQL injection prevention (Prepared Statements)

### Performance
- [ ] Увеличить Redis memory до 512MB-1GB
- [ ] Настроить connection pooling (HikariCP)
- [ ] Добавить CDN для статики
- [ ] Gzip compression для API responses
- [ ] Database indexes review

### Monitoring
- [ ] Prometheus + Grafana для метрик
- [ ] Sentry для error tracking
- [ ] Structured logging (JSON)
- [ ] Health checks (/q/health)
- [ ] Redis hit rate monitoring

### Reliability
- [ ] Redis persistence (AOF/RDB)
- [ ] PostgreSQL backups (pg_dump)
- [ ] Graceful shutdown
- [ ] Circuit breaker для GigaChat API
- [ ] Retry logic с exponential backoff

---

Документ актуален на: **Февраль 2026**
